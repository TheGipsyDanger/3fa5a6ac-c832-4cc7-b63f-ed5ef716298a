query getAnimesPerPage($page: Int, $perPage: Int) {
  Page(page: $page, perPage: $perPage) {
    pageInfo {
      total
      currentPage
      hasNextPage
    }
    media {
      id
      coverImage {
        large
      }
      title {
        english
        native
      }
      format
      seasonYear
      status
      episodes
    }
  }
}
