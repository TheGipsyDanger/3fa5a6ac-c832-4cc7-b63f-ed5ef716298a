query getAnimesPerPage($page: Int, $perPage: Int) {
  Page(page: $page, perPage: $perPage) {
    pageInfo {
      currentPage
      hasNextPage
    }
    media {
      id
      averageScore
      coverImage {
        large
      }
      tags {
        id
        name
        category
      }
      title {
        english
        native
      }
      format
      seasonYear
      status
      episodes
    }
  }
}
