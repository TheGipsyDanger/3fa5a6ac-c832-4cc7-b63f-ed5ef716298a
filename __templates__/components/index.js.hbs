import { I{{pascalCase name}} } from '@/components/{{pascalCase name}}/{{pascalCase name}}.types'

export const {{pascalCase name}} = (props: I{{pascalCase name}}.IView) => (
  <div data-testid={`{{pascalCase name}}`}>
    <p>{`{{pascalCase name}}`}</p>
  </div>
)